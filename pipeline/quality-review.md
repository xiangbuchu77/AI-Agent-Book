# Quality Review

## Required Checks

- All chapter directories contain `index.md`, `diagram.mmd`, and `example.py`.
- Markdown contains all required chapter sections.
- Mermaid diagrams use valid fenced code blocks.
- Python examples run with the standard Python interpreter.
- The repository can be committed and pushed without generated temporary files.
